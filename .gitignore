node_modules/
out/
dist/
release/
# build/ contiene l'icona, che va versionata: si ignorano solo gli artefatti
build/*.ico
.vite/
*.log
.DS_Store
# Configurazione dell'updater usata solo con CM_UPDATE_DEV=1: punta a una
# build locale e non deve finire nel repository.
dev-app-update.yml
